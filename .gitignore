# Dependencies
node_modules/

# Environment & secrets
.env
.env.*
!.env.example
.envrypt

# SSH keys
vps-key
vps-key.pub
*.pem
*.key
id_rsa*
id_ed25519*

# User config (contains RPC URL, wallet key, API keys)
user-config.json

# Runtime state (position tracking, lessons — user-specific)
state.json
lessons.json
smart-wallets.json
pool-memory.json
token-blacklist.json
strategy-library.json
decision-log.json
hivemind-cache.json
signal-weights.json

# Logs
logs/

# OS
.DS_Store

# Dev context
context.md
CONTEXT.md
dev-blocklist.json
docs/

# Circuit breaker runtime state
circuit-breaker-state.json

# Discord listener queue
discord-signals.json

# Playwright MCP browser logs
.playwright-mcp/

# Claude Code runtime lock
.claude/scheduled_tasks.lock

# Backup files
*.bak
*.bak2
*.bak3
*.bak4
*.bak5

# Runtime state files (potentially sensitive)
signal-results.jsonl
paper-trades.json
llm-usage.json
auto-screener-state.json

# Signal pipeline directories
signals/inbox/
signals/processed/
signals/rejected/
signals/history/

# Intel crawler output (Sirius — crawled social/chat content, keep out of git)
intel/x/
intel/telegram/
intel/discord/
intel/images/

# Backtest reports
backtest-reports/

# Snapshot publisher runtime artifacts
status-snapshot.json
.git-worktrees/

# Agent bundles / archives
.agent-bundle/
*.zip

# Lyra journal runtime ledger (local state, not source)
journal.jsonl
same-token-cooldown-shadow.jsonl
